<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme - used for splash screen -->
    <style name="Theme.ReadSphere.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.ReadSphere</item>
    </style>

    <!-- Post-splash theme (overridden in code by Jetpack Compose) -->
    <style name="Theme.ReadSphere" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
